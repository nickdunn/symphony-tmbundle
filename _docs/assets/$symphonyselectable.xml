<?xml version="1.0"?>







<class type="class" name="$.symphonySelectable" handle="$.symphonySelectable">
	
	<package name="assets" handle="assets"/>
	
	
		<location>assets/symphony.selectable.js</location>
	
	
	<modifiers>
		<modifier>public</modifier>
	</modifiers>
	
	<!--description>
		<p></p>
	</description-->
	
			
		<description>
			<p>This plugin makes items selectable. Clicking an item will select it 
by adding the class <code>.selected</code>. Holding down the shift key 
while clicking multiple items creates a selection range. Holding the meta key 
(which is <code>cmd</code> on a Mac or <code>ctrl</code> on Windows) allows 
the selection of multiple items or the modification of an already selected 
range of items. Doubleclicking outside the selection list will 
remove the selection.</p>
			
				
					<pre class="code">
			var selectable = $('table').symphonySelectable();
			selectable.find('a').mousedown(function(event) {
				event.stopPropagation();
			});</pre>
				
			
		</description>
	
	
	
	
	
	
	
	<methods>
	
		
	
		<method
			name="symphonySelectable"
			 
			
		>
		
			<modifiers>
				<modifier>public</modifier><modifier>static</modifier>
			</modifiers>
			
			<location>assets/symphony.selectable.js</location>
			
			<description>
				<p>This plugin makes items selectable. Clicking an item will select it 
by adding the class <code>.selected</code>. Holding down the shift key 
while clicking multiple items creates a selection range. Holding the meta key 
(which is <code>cmd</code> on a Mac or <code>ctrl</code> on Windows) allows 
the selection of multiple items or the modification of an already selected 
range of items. Doubleclicking outside the selection list will 
remove the selection.</p>
				
					
						<pre class="code">
			var selectable = $('table').symphonySelectable();
			selectable.find('a').mousedown(function(event) {
				event.stopPropagation();
			});</pre>
					
				
			</description>
			
			<tags>
				
				
				
				
				
				
				
				
				
				
					
						<tag group="Parameters" name="custom_settings">
							<description>
								<p>An object specifying containing the attributes specified below</p>
							</description>
						</tag>
					
						<tag group="Parameters" name="custom_settings.items">
							<description>
								<p>Selector to find items that are selectable</p>
							</description>
						</tag>
					
						<tag group="Parameters" name="custom_settings.handles">
							<description>
								<p>Selector to find children that can be clicked to select the
item. Needed to properly handle item highlighting when used in connection with the orderable plugin</p>
							</description>
						</tag>
					
				
				
			</tags>
			
			
				<parameters>
					
						<parameter name="custom_settings" type="Object" />
					
						<parameter name="custom_settings.items" type="String" />
					
						<parameter name="custom_settings.handles" type="String" />
					
				</parameters>
			
			
		</method>
		
		
		
		
		
	</methods>
			
	
	
	
</class>